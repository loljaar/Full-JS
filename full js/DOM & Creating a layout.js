console.log("DOM & Creating a layout");



/*

let a = document;
a=document.all;
 a= document.body;
    a= document.forms; 
    Array.from(a).forEach(element => {
        console.log(element);
    });
    a=document.links[0].href;    

    console.log(a);

*/

/*
    A=document.images;
    Array.from(A).forEach(element => {
        console.log(element);
    });
    
    b= document.scripts;
    Array.from(b).forEach(element => {
        console.log(element);
    });
    
    

    let text = "harry";
    c=document.links;

    Array.from(c).forEach(element => {
        let H = element.href;
        if (H.includes(text)) {
            
            console.log(H);
        }
    });



    let  text = "jatin";
    a = document.links;
    Array.from(a).forEach(element => {
             let H = element.href;
             if (H.includes(text)) {
                console.log(H);
             }
    });
  */ 
    let T = "jatin"
    b= document.links;
    Array.from(b).forEach(element => {
        let H = element.href;
        if (H.includes(T)) {
            
            console.log(H);
        }
    });
